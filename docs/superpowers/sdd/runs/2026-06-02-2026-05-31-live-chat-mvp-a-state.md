# SDD Run State - live-chat-mvp-a

> Auto-generated by `docs/superpowers/sdd/scripts/sdd_run.py`, then task matrix hand-filled by main-agent (脚本仅识别 `T00x`，本 plan 用 `## Task N`，故手动补全)。

## Run Metadata

| Key | Value |
| --- | --- |
| Run ID | `2026-06-02-live-chat-mvp-a` |
| Topic | `live-chat-mvp-a` |
| Goal | `直播间级弹幕 MVP（M1 双 Room 抽象 + M2 弹幕全链路）` |
| Mode | `subagent-driven` |
| Branch | `feat/live-chat-mvp` |
| Worktree | `/Users/bytedance/.config/superpowers/worktrees/dy-ai-live-auction-fullstack-cc/feat-live-chat-mvp` |
| Base Branch | `main` |
| Started At | `2026-06-02 03:25` |
| Owner | `main-agent` |
| Status | `active` |

## Input Documents

| Type | Path | Required | Loaded |
| --- | --- | --- | --- |
| Agent Rules | `AGENTS.md` | yes | yes |
| SDD Runbook | `docs/superpowers/sdd/RUNBOOK.md` | yes | yes |
| State Template | `docs/superpowers/sdd/state-template.md` | yes | yes |
| Plan | `docs/superpowers/plans/2026-05-31-live-chat-mvp-plan-a.md` | yes | yes |
| Tasks | `docs/superpowers/plans/2026-05-31-live-chat-mvp-plan-a.md`（内嵌） | yes | yes |
| Spec | `docs/superpowers/specs/2026-05-31-live-chat-and-price-flair-design.md` | yes | yes |
| Scope | `-` | no | no |

## Execution Summary

| Metric | Value |
| --- | --- |
| Total Tasks | `13` |
| Done | `12` |
| Blocked | `0` |
| In Progress | `0` |
| Pending | `1` |
| Last Updated | `2026-06-02 (T13 阶段A/阶段B环境链路验证完成；code review 3项问题已TDD修复；阶段B用户浏览器复测待确认)` |

## Task Matrix

| Task ID | Title | Status | Owner | Parallel Group | Depends On | Scope | Allowed Files |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `T1` | 扩展 WS 消息协议（DTO + 错误码） | `done` | `main-agent` | `W1` | `-` | backend ws | `backend/auction/websocket/message.go` |
| `T2` | 实现 ChatFilter（黑词与长度校验） | `done` | `subagent-W2` | `W2` | `T1` | backend ws | `backend/auction/websocket/chat_filter.go`, `chat_filter_test.go` |
| `T3` | 实现 ChatThrottle（Redis 双层频控） | `done` | `subagent-W2` | `W2` | `T1` | backend ws | `backend/auction/websocket/chat_throttle.go`, `chat_throttle_test.go` |
| `T4` | 实现 LiveStreamRoom（含环形缓冲） | `done` | `subagent-W2` | `W2` | `T1` | backend ws | `backend/auction/websocket/livestream_room.go`, `livestream_room_test.go` |
| `T5` | Hub 集成 LiveStreamRoom | `done` | `main-agent` | `W3` | `T4` | backend ws | `backend/auction/websocket/hub.go`, `client.go`, `handler/ws.go`(临时签名) |
| `T6` | 实现 handleChatSend 业务编排 | `done` | `main-agent` | `W4` | `T2,T3,T5` | backend ws | `backend/auction/websocket/chat_handler.go`, `chat_handler_test.go`, `client.go` |
| `T7` | WS 握手解析 live_stream_id 并装配 ChatHandler（关键检查点） | `done` | `main-agent` | `W5` | `T6` | backend handler | `backend/auction/handler/ws.go`, `backend/auction/main.go`, `client.go` |
| `T8` | 前端 WebSocketService 扩展（订阅 + 发送） | `done` | `main-agent` | `W6` | `T1` | h5 service | `frontend/h5/src/services/websocket.ts` |
| `T9` | 前端 liveChatStore（Zustand） | `done` | `main-agent` | `W6` | `-` | h5 store | `frontend/h5/src/store/liveChatStore.ts`, `__tests__/liveChatStore.test.ts` |
| `T10` | ChatBubble 组件 | `done` | `main-agent` | `W7` | `T9` | h5 component | `frontend/h5/src/components/LiveChat/ChatBubble.tsx`, `ChatPanel.module.css`, `__tests__/ChatBubble.test.tsx` |
| `T11` | ChatPanel 组件（输入 + 滚动列表） | `done` | `main-agent` | `W7` | `T9,T10` | h5 component | `frontend/h5/src/components/LiveChat/ChatPanel.tsx`, `__tests__/ChatPanel.test.tsx` |
| `T12` | 在 Live 页面集成 ChatPanel（关键检查点） | `done` | `main-agent` | `W8` | `T8,T11` | h5 page | `frontend/h5/src/pages/Live/index.tsx` |
| `T13` | 端到端冒烟（手动） | `pending` | `user` | `W9` | `T1-T12` | manual | `-` |

## Wave Plan

| Wave | Goal | Tasks | Start Condition | Completion Condition |
| --- | --- | --- | --- | --- |
| `W1` | 协议先行：消息类型、错误码、DTO | `T1` | state 初始化 | message.go 编译通过 |
| `W2` | 后端原子能力（独立文件，可并行） | `T2,T3,T4` | T1 done | 各自单测 PASS |
| `W3` | Hub 接入 LiveStreamRoom | `T5` | T4 done | `go test ./websocket` PASS |
| `W4` | 业务编排 ChatHandler | `T6` | T2,T3,T5 done | ChatHandler 单测 PASS |
| `W5` | 握手装配（后端闭环 checkpoint） | `T7` | T6 done | `go test ./...` PASS + 主 agent review |
| `W6` | 前端通道与状态（可并行） | `T8,T9` | T1 done | 现有 ws 测试不破坏 / store 单测 PASS |
| `W7` | 前端组件 | `T10,T11` | T9 done | 组件单测 PASS |
| `W8` | Live 页面集成（前端闭环 checkpoint） | `T12` | T8,T11 done | LiveRoom 测试不破坏 + 主 agent review |
| `W9` | 端到端冒烟 | `T13` | T1-T12 done | 用户手动核查 |

## Task Records

### T1 - 扩展 WS 消息协议

| Key | Value |
| --- | --- |
| Status | `pending` |
| Owner | `unassigned` |
| Depends On | `-` |
| Parallel Group | `W1` |

**TDD Plan**: 仅 DTO/常量，靠 `go build ./...` 验证编译；无独立单测（Task 2-6 间接覆盖）。

**Verification Evidence**

| Command | Expected | Actual | Result |
| --- | --- | --- | --- |
| `cd backend/auction && go build ./...` | 编译通过 | `EXIT=0 编译通过` | `done` |

**Commit**: `feat(ws): add chat_send/chat_message DTOs and error codes`

---

### T2 - ChatFilter

| Key | Value |
| --- | --- |
| Status | `pending` |
| Owner | `unassigned` |
| Depends On | `T1` |
| Parallel Group | `W2` |

**TDD Plan**: 先写 `chat_filter_test.go`（长度边界 + 黑词大小写不敏感）确认 FAIL，再实现。

**Verification Evidence**

| Command | Expected | Actual | Result |
| --- | --- | --- | --- |
| `go test ./websocket -run TestChatFilter -v` | PASS | `Red: undefined NewChatFilter; Green: 8 子用例 PASS` | `done` |

**Commit**: `feat(ws): add ChatFilter for length and blocked-word validation`

---

### T3 - ChatThrottle

| Key | Value |
| --- | --- |
| Status | `pending` |
| Owner | `unassigned` |
| Depends On | `T1` |
| Parallel Group | `W2` |

**TDD Plan**: 用 miniredis 写用户级/房间级/TTL 三组失败测试，再实现 Redis 双层频控。

**Verification Evidence**

| Command | Expected | Actual | Result |
| --- | --- | --- | --- |
| `go test ./websocket -run TestChatThrottle -v` | PASS | `Red: undefined NewChatThrottle; Green: UserLimit/RoomLimit/TTLReset PASS（新增 miniredis v2.38.0）` | `done` |

**Commit**: `feat(ws): add Redis-based dual-layer chat throttling`

---

### T4 - LiveStreamRoom

| Key | Value |
| --- | --- |
| Status | `pending` |
| Owner | `unassigned` |
| Depends On | `T1` |
| Parallel Group | `W2` |

**TDD Plan**: 先写广播投递 / 环形缓冲 100 上限 / 进房回放三组失败测试，再实现 Room。

**Verification Evidence**

| Command | Expected | Actual | Result |
| --- | --- | --- | --- |
| `go test ./websocket -run TestLiveStreamRoom -v` | PASS | `Red: undefined NewLiveStreamRoom; Green: BroadcastDelivers/RingBufferReplay/RegisterReplaysHistory PASS。全量 ./websocket: 17 PASS, 1 SKIP(需真实Redis), 0 FAIL` | `done` |

**Commit**: `feat(ws): add LiveStreamRoom with ring-buffer history replay`

---

### T5 - Hub 集成 LiveStreamRoom

| Key | Value |
| --- | --- |
| Status | `done` |
| Owner | `main-agent` |
| Depends On | `T4` |
| Parallel Group | `W3` |

**TDD Plan**: Client 增 LiveStreamID/UserName；NewClient 签名变更后修复调用方编译；跑全量 ws 测试回归。

**Verification Evidence**

| Command | Expected | Actual | Result |
| --- | --- | --- | --- |
| `go build ./...` | 编译通过 | `EXIT=0 编译通过（ws.go:84 临时签名 NewClientSimple(conn,auctionID,userID,0,"")）` | `done` |
| `go test ./websocket -v` | PASS（含既有锁/状态测试） | `全量 PASS, 1 SKIP(TestStateManager_Integration 需真实Redis), 0 FAIL` | `done` |

**Commit**: `feat(ws): wire LiveStreamRoom into Hub with auto register/unregister` (b35dec8e)

---

### T6 - ChatHandler 业务编排

| Key | Value |
| --- | --- |
| Status | `done` |
| Owner | `main-agent` |
| Depends On | `T2,T3,T5` |
| Parallel Group | `W4` |

**TDD Plan**: 先写 guest 拒绝 / 长度 / 黑词 / happy path / 频控五组失败测试，再实现 Handle + handleMessage 接入。

**Verification Evidence**

| Command | Expected | Actual | Result |
| --- | --- | --- | --- |
| `go test ./websocket -run TestChatHandler -v` | PASS（5 用例） | `Red: undefined ChatHandler/NewChatHandler; Green: GuestRejected/LengthError/BlockedWord/HappyPath/RateLimit 全 PASS` | `done` |
| `go test ./websocket -v` | 全量 PASS | `全量 PASS, 1 SKIP(需真实Redis), 0 FAIL` | `done` |

**Commit**: `feat(ws): orchestrate chat_send via ChatHandler with filter+throttle+broadcast` (c2d60d91)

---

### T7 - 握手解析 + 装配（后端 checkpoint）

| Key | Value |
| --- | --- |
| Status | `done` |
| Owner | `main-agent` |
| Depends On | `T6` |
| Parallel Group | `W5` |

**TDD Plan**: 高风险——ws.go 内嵌 read 循环替换为 Client.ReadPump/WritePump，需确认不破坏既有 ping/sync_request e2e；main.go 装配 ChatHandler。无新单测，靠全量回归 + 主 agent review 把关。

**Plan 偏离修正（第一性原理）**: plan Task 7 有两处会导致运行时故障，已在实现中修正：
1. `NewClientSimple` 创建的 client 未设置 `hub` 字段，而 `ReadPump` 的 defer 调用 `c.hub.Unregister <- c`。直接启动会 nil panic。→ 新增 `Client.SetHub()` 并在 ws.go 装配前调用。
2. plan 让 welcome 消息"直接 conn.WriteMessage"且并行启动 WritePump，但 gorilla websocket 禁止并发写同一连接。→ welcome 在 WritePump 启动**之前**同步写出，保证无并发。
3. registerClient 已含 live_stream 双注册钩子（T5），故 ReadPump 路径自动复用，注销也走 Hub.Unregister → unregisterClient 钩子。

**Verification Evidence**

| Command | Expected | Actual | Result |
| --- | --- | --- | --- |
| `go build ./...` | 编译通过 | `EXIT=0` | `done` |
| `go test ./... -count=1` | 全量 PASS | `auction-service 全部包 ok（含 handler/service/websocket），0 FAIL` | `done` |

**Commit**: `feat(ws): parse live_stream_id at handshake and wire ChatHandler` (b763c09e)

---

### T8 - 前端 WebSocketService 扩展

| Key | Value |
| --- | --- |
| Status | `done` |
| Owner | `main-agent` |
| Depends On | `T1` |
| Parallel Group | `W6` |

**TDD Plan**: 扩展 constructor + sendChat + chat_message 分发；不破坏既有 websocket 测试。

**实现说明**: 新增 `liveStreamId` 字段；constructor 三参数 `(auctionId, token?, liveStreamId?)`；connect() URL 保持原字符串拼接风格追加 `&live_stream_id=`（仅存在时）；新增 `sendChat(text, clientMsgId)`。`chat_message` 走现有 handlers map 通用分发，**handleMessage 无需改动**（plan Task 8 Step 5 的免改判断成立）。

**Verification Evidence**

| Command | Expected | Actual | Result |
| --- | --- | --- | --- |
| `npm test -- --testPathPatterns=websocket` | PASS | `9/9 PASS（含 sync_request/reconnect/notification e2e 不破坏）。注：Jest 30 选项已由 testPathPattern 改为 testPathPatterns` | `done` |

**Commit**: `feat(h5): WebSocketService supports live_stream_id and sendChat` (97f85030)

---

### T9 - liveChatStore（Zustand）

| Key | Value |
| --- | --- |
| Status | `done` |
| Owner | `main-agent` |
| Depends On | `-` |
| Parallel Group | `W6` |

**TDD Plan**: 先写 200 上限 / cooldown 失败测试，再实现 store。

**依赖说明**: 项目原未装 zustand（store 全用 React Context）。本任务安装 `zustand@^5.0.14`（jest preset=ts-jest，zustand v5 提供 CJS 入口，无需额外 transform 配置即可被 jest 解析）。选型理由：chat_message 在 wsService 回调（组件树外）到达，需 `getState().receive()` 组件外 dispatch。

**Verification Evidence**

| Command | Expected | Actual | Result |
| --- | --- | --- | --- |
| `npm test -- --testPathPatterns=liveChatStore` | Red 后 Green | `Red: Cannot find module '../liveChatStore'; Green: 2/2 PASS（200 上限+history[0].user_id===50 / cooldown）` | `done` |

**Commit**: `feat(h5): add liveChatStore with history cap and cooldown` (含 package.json/lock zustand 依赖)

---

### T10 - ChatBubble 组件

| Key | Value |
| --- | --- |
| Status | `done` |
| Owner | `main-agent` |
| Depends On | `T9` |
| Parallel Group | `W7` |

**TDD Plan**: 先写渲染 / XSS 文本不解析 / self 标记失败测试，再实现组件 + 样式。

**Verification Evidence**

| Command | Expected | Actual | Result |
| --- | --- | --- | --- |
| `npm test -- --testPathPatterns=ChatBubble` | Red 后 Green | `Red: Cannot find module '../ChatBubble'; Green: 3/3 PASS（renders/XSS not interpreted/bubbleSelf class）` | `done` |

**Commit**: `feat(h5): add ChatBubble component with XSS-safe text rendering`

---

### T11 - ChatPanel 组件

| Key | Value |
| --- | --- |
| Status | `done` |
| Owner | `main-agent` |
| Depends On | `T9,T10` |
| Parallel Group | `W7` |

**TDD Plan**: 先写空禁用 / 超长拒绝 / 发送+cooldown / store 渲染失败测试，再实现 Panel。

**Verification Evidence**

| Command | Expected | Actual | Result |
| --- | --- | --- | --- |
| `npm test -- --testPathPatterns=ChatPanel` | Red 后 Green | `Red: Cannot find module '../ChatPanel'; Green: 4/4 PASS（disable empty/reject >50/send+cooldown via fakeTimers/render from store）` | `done` |

**Commit**: `feat(h5): add ChatPanel with input validation, cooldown and scrolling list`

---

### T12 - Live 页面集成（前端 checkpoint）

| Key | Value |
| --- | --- |
| Status | `done` |
| Owner | `main-agent` |
| Depends On | `T8,T11` |
| Parallel Group | `W8` |

**TDD Plan**: 先读现有 Live 页确认 liveStreamId/user/wsService 来源，再挂载 ChatPanel + chat_message handler；不破坏既有 LiveRoom 测试。

**实现说明（第一性原理）**:
- WS 构造改为三参 `new WebSocketService(auctionId, token, queryLiveStreamId || undefined)`。选 `queryLiveStreamId`（route `?id=`）而非 `effectiveLiveStreamId`：握手发生在 effect 内，需同步可得的 live_stream_id，避免等待 auction 详情异步返回再重连。
- 用 `wsRef` 持有实例供 `ChatPanel.onSend` 调 `sendChat`，避免把 ws 提升为 state 触发重渲染。
- `chat_message` handler 在组件树外用 `useLiveChatStore.getState().receive(data)` dispatch（验证了 zustand 选型的必要性）。
- cleanup 中 `off('chat_message')` + `store.reset()`，防止换房残留历史。
- 现有 LiveRoom 测试断言旧 WS 契约（2 参、无 off/sendChat），按 TDD 先更新测试（Red）再实现（Green）：mock 补 `off`/`sendChat`，构造断言加第三参 `3`，新增 chat 集成用例。

**Verification Evidence**

| Command | Expected | Actual | Result |
| --- | --- | --- | --- |
| `npm test -- --testPathPatterns=LiveRoom` | Red 后 Green | `Red: 2 failed(构造参数+chat handler undefined); Green: 6/6 PASS` | `done` |
| `npm test`（全量前端 checkpoint） | 全量 PASS | `37 suites / 204 tests PASS, 0 FAIL` | `done` |
| `npx tsc --noEmit` | 0 类型错误 | `EXIT=0` | `done` |

**Commit**: `feat(h5): mount ChatPanel in Live page and dispatch chat_message` (42c4079d)

---

### T13 - 端到端冒烟（手动）

| Key | Value |
| --- | --- |
| Status | `phase-B-env-ready`（阶段 A 后端 WS 直连冒烟全绿；阶段 B HTTP/WS dev 链路已验证，待用户浏览器复测结论） |
| Owner | `user` |
| Depends On | `T1-T12` |
| Parallel Group | `W9` |

**TDD Plan**: 人工核查，不写自动化。两段式：阶段 A 用 websocat 双连后端 WS（8083 `/ws`）直连验证 T1-T7 协议；阶段 B `npm run dev` 浏览器双标签经 vite WS 代理复测 UI。

**冒烟前发现并修复的偏离（根因修复，已 commit）**

1. **端口默认值缺冒号导致 dev fallback 启动崩溃**（`backend/auction/config/config.go`）：DefaultConfig 的 `HTTPPort:"8082"` / `WSPort:"8083"` 缺前缀冒号，Hertz `WithHostPorts` 与 stdlib `http.Server.Addr` 均 panic/fatal（`missing port`）。第一性原理修复：源头改为 `:8082` / `:8083`，HTTP+WS 单点统一。
2. **vite dev proxy 未代理 WS**（`frontend/h5/vite.config.ts`）：在 `/api` 之前新增 `/api/v1/ws` → `ws://localhost:8083` 且 `ws:true`；因后端 WS 路由实为 `/ws`，加 `rewrite` 将 `/api/v1/ws` 重写为 `/ws`，打通阶段 B H5→auction WS 直连。

**阶段 A 冒烟证据（websocat 1.14.1，auction worktree 最新代码，MySQL/Redis Running）**

WS 路由实测为 `ws://localhost:8083/ws?auction_id=&live_stream_id=&token=`（非 `/api/v1/ws`）。账号 Alice(1002)/Bob(1003)。

| 用例 | 操作 | Expected | Actual | Result |
| --- | --- | --- | --- | --- |
| 基础收发+广播回送 | A 发 `chat_send` | 发送者收到自己的 `chat_message` | 收到 user_id=1002 chat_message | `pass` |
| 跨连接广播 | A 发，B 同 ls=3 在线 | B 收到 A 的消息 | B 收到 user_name=Alice 消息 | `pass` |
| 历史回放 | A/B 后连接 | 连接后先收到此前历史消息 | 新连接均先收到 hello world/hi from A | `pass` |
| 黑词过滤 | A 发"加我微信好吗" | code 40002 | `{"code":40002,"message":"blocked word detected"}` | `pass` |
| 长度超限 | A 发 60 字符 | code 40001 | `{"code":40001,"message":"text length invalid"}` | `pass` |
| 用户频控 | A 1s 内连发 2 条 | 第 2 条 code 40003 | 第1条广播成功，第2条 `{"code":40003,"message":"rate limited"}` | `pass` |
| 房间隔离 | B 在 ls=99，A 在 ls=3 发 | B 收不到 | B 仅收到 system，无 ls=3 消息 | `pass` |

**阶段 B 环境链路证据**

| Command | Expected | Actual | Result |
| --- | --- | --- | --- |
| `curl http://localhost:5174/api/v1/auctions/5` | 经 vite -> gateway -> auction 返回 200 | 返回 auction_id=5 / live_stream_id=3 | `pass` |
| `curl http://localhost:5174/api/v1/live-streams/3` | 经 vite -> gateway -> product 返回 200 | 返回 live_stream_id=3 | `pass` |
| `websocat ws://localhost:5174/api/v1/ws?...` | 经 vite rewrite -> auction `/ws`，可收发 chat | 收到 system + `chat_message(ui-proxy-1)` | `pass` |

**Code Review Fixes（TDD）**

| Issue | Fix | RED | GREEN |
| --- | --- | --- | --- |
| WS `user_id` fallback 可伪造弹幕身份 | `Client.Authenticated` 仅由 JWT 设置；`ChatHandler` 发送弹幕必须 `Authenticated=true` | `TestChatHandler_QueryUserIDFallbackRejected` 先失败（no error reply） | `go test ./websocket` / `go test ./...` PASS |
| `RegisterToLiveStream` 异步注册导致刚连接即发送可能丢广播 | Hub 注册/注销 live stream room 改为同步可见；room 仍由 Run 处理广播 | `TestHub_RegisterToLiveStreamIsImmediatelyVisible` 先失败（client count=0） | `go test ./websocket` / `go test ./...` PASS |
| 前端 `sendChat` 静默失败但 UI 清空输入并 cooldown | `sendChat` / `onSend` 返回 boolean；只有成功发送才 `markSent` + 清空 | `ChatPanel keeps text editable when send fails` 先失败（input 被清空） | `npm test -- --runInBand` 37 suites / 205 tests PASS；`npx tsc --noEmit` PASS |

## Final Review Checklist

- [x] State file was created before subagent dispatch.
- [x] Every implementation task records TDD Red -> Green -> Verify evidence.
- [x] Every completed subagent response starts with `当前分支/worktree：`.
- [x] Verification commands and results are recorded.

## Final Handoff

当前分支/worktree：feat/live-chat-mvp @ /Users/bytedance/.config/superpowers/worktrees/dy-ai-live-auction-fullstack-cc/feat-live-chat-mvp

**状态**

- `phase-B-env-ready`：T1-T12 全部 done；T13 阶段 A 后端 WS 冒烟全绿；阶段 B dev HTTP/WS 链路已验证，待用户浏览器复测结论。
- Code review 发现的 3 个问题已按 TDD 修复：JWT-only 弹幕身份、live stream room 同步注册、前端发送失败不清空输入。
- 后端全量 `go test ./...` PASS；前端全量 `npm test -- --runInBand` 37 suites / 205 tests PASS；`npx tsc --noEmit` 0 错误。
