* [x] 已创建页面映射文档，覆盖新移动端所有页面和旧 H5 所有页面。

* [x] 每个新移动端页面都被标记为“替换旧页面”或“新增保留页面”。

* [x] 每个旧 H5 页面都被标记为“被替换”或“从用户可达路径舍弃”。

* [x] 迁移顺序遵守逐页闭环规则，不存在未完成接口对接就进入下一页的情况。

* [x] H5 全局运行时保留认证、Toast、错误边界、网关 `/api/v1` 请求和现有服务封装约定。

* [x] `Home` 主页已使用新移动端 UI，并完成页面所需接口和按钮对接。

* [x] `LiveRoom` 直播间已使用新移动端 UI，并完成直播、竞拍、出价、关注、实时状态相关对接。

* [x] `ProductDetail` 商品详情已作为替换或新增页面保留，并完成所需接口和跳转对接。

* [x] `AuctionResult` 竞拍结果页已使用新移动端 UI，并完成结果、订单或支付相关对接。

* [x] `Profile` 个人中心已使用新移动端 UI，并完成用户资料、余额和入口跳转对接。

* [x] `AuctionHistory` 竞拍历史已使用新移动端 UI，并完成历史数据接口或缺失接口记录。

* [x] `Following` 关注列表已使用新移动端 UI，并完成关注列表、取消关注和直播间跳转对接。

* [x] `Notifications` 消息通知已作为替换或新增页面保留，并完成通知接口或缺失接口记录。

* [x] `Login` 登录页已使用新移动端 UI，并保留登录、token 存储、401 回跳和认证上下文行为。

* [x] 新移动端有但旧 H5 没有的页面已保留并接入 H5 路由。

* [x] 旧 H5 有但新移动端没有的页面已从用户可达导航或路由中舍弃，但源码未在未经确认前删除。

* [x] 旧接口冗余项已记录到 `frontend/h5/docs/mobile-ui-migration/redundant-interfaces.md`。

* [x] 新 UI 需要但现有后端缺失的接口已记录到 `frontend/h5/docs/mobile-ui-migration/missing-interfaces.md`。

* [x] 接口响应与新 UI 数据结构不匹配的情况已完成前端适配或记录为后端契约差异。

* [x] 迁移进度和每页验证结果已记录到 `frontend/h5/docs/mobile-ui-migration/migration-progress.md`。

* [x] `npm run build` 在 `frontend/h5` 下通过，或失败原因被记录且确认不是本次迁移引入。

* [x] 关键单测或 e2e 测试已运行，或因环境/既有问题无法运行时已记录命令、错误摘要和影响范围。

* [x] 最终响应用户时明确说明迁移已完成，并询问是否删除旧移动端代码。
